<?php
// create_db.php: removed — DB was created during setup and this helper is no longer needed.
// If you need to recreate the DB/user manually, restore this file from VCS or run SQL manually.
// Intentionally left as a placeholder to avoid accidental execution.

exit(0);
